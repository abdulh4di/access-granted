# Copy to .env.local and fill in. .env.local is gitignored.

# Web3Forms access key the contact form submits to — get one at web3forms.com.
# Public by design (it ships in the browser bundle); it only permits sending to
# the address the key is registered to.
NEXT_PUBLIC_WEB3FORMS_KEY=
